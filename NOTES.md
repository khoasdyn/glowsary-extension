# Notes and open mismatches

This file is the running log of mismatches and open gaps across the project: between the UI and the design, between Figma and the code tokens, between a request and PRD.md, between PRD.md and DESIGN.md, and anything else that does not line up yet. Claude records items here so nothing is lost while a decision is pending. Each item stays until it is resolved, then it is marked done or removed.

How to read an item: the area it touches, what the mismatch or gap is, and its status. Open means it still needs a decision or a fix. Resolved means it is settled.

## Open

21. The Add/Edit form changed from a centered modal to a right-side panel. PRD.md was updated: the "Management form modal" component is now the "Management form panel" (right edge, full height, 16px margin all edges, closes with X or Escape, no Cancel, no backdrop close), and FR-17a, FR-18, and FR-18a reflect this (decision 2026-06-08). DESIGN.md and the code still describe the old centered modal and the old name, so they need a design pass and a build change to match. Area: PRD.md versus DESIGN.md and code. Status: open; PRD updated, design and build to follow.
24. The color picker is a new feature with ripple beyond the form. Color is now per-entry data (data model), it is shown on the entry card (FR-17g), and it adds a fourth CSV column (FR-35, FR-36). Build must keep old three-column CSV files importable (color falls back to the first color) and must not let color into the import duplicate check (FR-36b keeps the existing entry's color). Area: PRD.md versus build and backward compatibility. Status: open; handled in the PRD, build to implement.
28. The Management form panel is a composition of components, now mostly specced. The panel is built from a header (title plus close), the Field input (Word, Aliases), the Multiline input (Definition), the Color picker, and a footer that uses the Text Button (Save) and the Icon Button (delete). As of 2026-06-08 the Color picker, the Field input, the Multiline input, and the Management form panel container are all specced in the DESIGN.md Components section, and the footer's Text Button and Icon Button already had entries. The design spec for the redesign is now complete. What remains is the build, which is build mode's job and is tracked under item 21 (code still has the old centered modal and must move to the right-side panel). Area: DESIGN.md and scope. Status: resolved 2026-06-08 on the design spec; the code change remains under item 21.
29. The panel has no open or close motion defined in Figma. The "Dialog" frame is static, so there is no slide-in, fade, or timing for how the right-side panel enters and leaves. The DESIGN.md panel entry says the build uses a simple, subtle entrance until Figma defines one. Area: Figma and motion. Status: open; waiting on a motion design decision from the user.
30. The built Color picker is missing the selected-circle border. The DESIGN.md Color picker entry says the selected circle has two marks in its family's 800 step: a 1px solid border and a check. The build (extension/color-picker.css) sets `border: 0` and adds no border on the selected state, so only the check shows. Found in build review 2026-06-08 from a screenshot. Area: UI versus design. Status: open; build fix needed to add the 1px family-800 border on the selected circle.
31. The built Color picker check does not use the check.svg asset. The repo has extension/assets/check.svg, the Figma check icon (a thin stroked tick). The build (extension/color-picker.js) instead draws a custom inline filled checkmark with a different path, so the shape does not match Figma. Two extra points: the build does recolor the check per family using the family 800 token (good), but the check.svg asset file hardcodes its stroke as #6B21A8 (purple 800), which breaks the "no raw values, use tokens" rule and would force every check to purple if the asset were used as is. So the fix is to use the check.svg shape with its color driven by the per-family 800 token (currentColor), not a fixed color. Area: UI versus design and code tokens. Status: open; build fix needed.
32. The built Management form panel has no overlay (scrim). The DESIGN.md panel entry and the Figma "Overlay" frame (node 5:255) put the panel over a full-screen scrim of black at 40% opacity. In the build, the full-screen layer (`.editor-layer`, the `#editor-panel` element) has no background, so the management view behind is not dimmed and the 16px gaps around the panel show white page instead of a dimmed wash. Found in build review 2026-06-08. Area: UI versus design and code tokens. Status: open; build fix needed to add the black 40% scrim (referencing the black primitive, since no overlay token exists yet).
33. The built panel and inputs use the wrong corner radius. The build uses an 8px radius on the panel and on the field inputs. The correct tokens are `--radius-card-md` (24px) on the panel and `--radius-box` (12px) on the field inputs. The 8px panel radius is one reason the panel reads as a flush rectangle rather than a floating card. Area: UI versus design. Status: open; build fix needed to use `--radius-card-md` on the panel and `--radius-box` on the inputs (covered in the radius-tokens build plan, item 36).
34. The built add mode still shows the delete button. PRD.md FR-17a and FR-18a say add mode has no delete button. The JS does set the delete button hidden in add mode (options.js sets `deleteEntryFromPanel.hidden = !entry`), but the CSS class `.icon-button` sets `display: inline-flex`, which overrides the `[hidden]` attribute, so the button stays visible. Area: UI versus design and code. Status: open; build fix needed so `[hidden]` wins for the icon button (for example `.icon-button[hidden] { display: none; }`).
36. The radius token file does not exist yet. `extension/tokens/radius-tokens.js` needs to be created with four tokens from the Figma radius export (2026-06-09): `--radius-cta` (9999px), `--radius-box` (12px), `--radius-card-sm` (12px), `--radius-card-md` (24px). Once created, the panel radius (item 33) and any other hardcoded radius values in the code should be updated to reference these tokens. Area: code tokens. Status: open; build needed.
37. Word Card semantic tokens do not exist in the code yet. The `semantic-color-tokens.js` needs a Word Card group added with five modes: Add New, Purple, Yellow, Green, Blue. Each mode defines five tokens: `--wc-bg-alias-chip`, `--wc-bg-card`, `--wc-border`, `--wc-subtext`, `--wc-word-text`. Values from the Figma Color Modes export (2026-06-09) are in the build plan. The card rendering logic also needs to scope the correct mode's tokens to each card based on its color value. Area: code tokens and word card rendering. Status: open; build needed.
38. Alias chip radius has no dedicated token. Figma defines no "small" chip radius, so alias chips currently fall back to `--radius-box` (12px). This is tracked as a known gap; if the chip radius should differ it must be added in Figma first. Area: Figma and design tokens. Status: open; waiting on Figma.
35. Outside-click close could not be reproduced in the current code. The user reported that clicking outside the panel closes it, which PRD.md says must not happen. In the current options.js the only closers are the X button and the Escape key; there is no click handler on the overlay or document that closes the panel. So the current code already matches the spec on this point. Either the tested build predates the latest code, or it needs a re-check. Area: UI versus design. Status: open; ask the user to re-verify on the latest build, and keep the no-backdrop-close guard in any fix.

8. Icon Button sizing has no tokens. The Icon Button is a fixed 40 by 40 box with an 18 by 18 icon, both temporary raw values in the Icon Button spec, because no spacing or sizing scale exists yet. Same debt as the Text Button (item 1) and the Switch (item 6). Area: DESIGN.md and code tokens. Status: open, debt; swap for tokens once the scale lands.
9. Icon Button has no interaction states. Figma defines only the three static variants (Primary, Secondary, Destructive). There is no hover, focus, pressed, or disabled state. Same gap as the Text Button (item 2) and the Switch (item 5). Area: Figma and UI. Status: open, waiting on the user to design these states in Figma.
10. Icon Button needs an accessible name and a tooltip. The button is icon-only with no visible text, so each use must pass an accessible name for screen readers and a tooltip for sighted users. Figma does not specify these. Area: UX and build. Status: open, handled as a build requirement in the plan; revisit if Figma later defines tooltip text.
11. Variant naming is inconsistent between Text Button and Icon Button. The Text Button calls its solid dark variant "Default"; the Icon Button calls the same role "Primary". This is a design-system naming gap that may confuse readers. Area: Figma and DESIGN.md. Status: open, waiting on the user to pick one name across both components in Figma.
13. The name "Icon Button" collides with an existing `icon-button` CSS class. The code already uses an `icon-button` class for the form modal's text "×" close button, which is a different thing from the new Icon Button component. When Codex builds the component, the naming must be sorted so the two do not clash, and it should be decided whether that close button later becomes a real Icon Button instance. Area: DESIGN.md and code. Status: open, to handle when the component is built and applied.
12. Icon Button and Text Button treat "Destructive" differently. The Icon Button's Destructive is a solid red circle (`--bg-error-solid` with `--fg-error-white`), while the Text Button's Destructive has no fill and shows plain red text (item 3). For a delete action the Icon Button reads much stronger. This may be intended or may need aligning once the Destructive look is decided. Area: Figma and UX. Status: open, linked to item 3; waiting on a design decision from the user.

5. Switch has no disabled, hover, pressed, or focus state in Figma. Figma defines only off and on. For accessibility the switch keeps a visible keyboard focus indicator until Figma defines a focus style. Area: Figma and UX. Status: open, waiting on a design decision from the user.
6. Switch sizing and spacing have no tokens. The track (54 by 28), padding (4 top and bottom, 6 left and right), and the knob (20 by 20) are temporary raw values in the Switch spec, because no spacing or sizing scale exists yet. Same debt as the Text Button (item 1). Area: DESIGN.md and code tokens. Status: open, debt; swap for tokens once the scale lands.

1. Text Button spacing and sizing have no tokens. The Text Button needs a fixed height (40), inner padding (20 left and right, 12 top and bottom), a gap between text and icon (8), and an icon size (20). None of these have tokens, because the spacing and sizing scale is not defined yet. Area: DESIGN.md and code tokens. Status: open, debt. The user chose to ship the button with these as temporary raw values for now (decision 2026-06-08). They must be swapped for tokens once a spacing and sizing scale is defined in Figma. The button is the place to revisit when that scale lands.
2. Text Button has no interaction states. Figma defines only four static states (Default, Secondary, Destructive, Disabled). There is no hover, focus, or pressed state. Area: Figma and UI. Status: open, waiting on the user to design these states in Figma.
3. Destructive Text Button variant has no background and no border. In Figma the Destructive state has a transparent background and no border, so it reads as plain red text and icon on the page. This may be intended, or it may need a border or a light red background. Area: Figma and UX. Status: open, waiting on a design decision from the user. Note: after the Text Button is applied to all buttons (decision 2026-06-08), the Delete buttons move from a solid red fill to this transparent red-text style, so Delete looks weaker. Revisit once the Destructive look is decided in Figma.

17. Tab nav sizing and spacing have no tokens. The track height (60), padding (6 on all sides), and gap (16) between items, plus each item's padding (16 left and right, 12 top and bottom), the gap (12) between icon and label, and the 24 by 24 icon slot, are temporary raw values in the Tab nav spec, because no spacing or sizing scale exists yet. Same debt as the Text Button (item 1), the Switch (item 6), and the Icon Button (item 8). Area: DESIGN.md and code tokens. Status: open, debt; swap for tokens once the scale lands.
18. Tab nav has no hover, focus, pressed, or disabled state. Figma defines only the two active states, Home active and Settings active, and the active tab is shown by the white fill alone. There is no hover, focus, pressed, or disabled style. For accessibility the Tab nav keeps a visible keyboard focus indicator until Figma defines a focus style. Same gap as the Text Button (item 2), the Switch (item 5), and the Icon Button (item 9). Area: Figma and UX. Status: open, waiting on a design decision from the user.
19. The full page-structure visuals are not specced. The page-structure Figma frame shows the whole management view on a white background, but the detailed screen layout (exact spacing, whether each section stays a card or sits flat on white, header and tab nav spacing) is a screen-level design, which design-planner does not cover yet (it is single-component scope) and product-planner does not own design values. The layout build plan captures structure, behavior, and the white background, but the finer page visuals still need a design pass against the page-structure frame. Area: design and the layout build plan. Status: open; confirm the page visuals against the Figma page-structure frame before or during the layout build.

## Resolved

23. How the entry card shows an entry's color is not decided. The user said this is not a concern right now ("no care now", 2026-06-08). PRD.md FR-17g left the exact look to a later design pass. Status: resolved 2026-06-09; Word Card color mode tokens (5 modes: Add New, Purple, Yellow, Green, Blue) define the card surface as a full tint in the assigned color. Specced in DESIGN.md Color section; build tracked in item 37.

22. The four color-picker colors map to existing primitive tokens, verified against Figma 2026-06-08: Purple 200 (#E9D5FF), Green 200 (#BBF7D0), Blue 200 (#BFDBFE), Yellow 200 (#FEF08A), and the check mark on the selected circle is Purple 800 (#6B21A8). No new color tokens are needed. The check color for the green, blue, and yellow selected states follows the family 800-step rule (Green 800, Blue 800, Yellow 800), confirmed by the user 2026-06-08. The Color picker is specced in the DESIGN.md Components section. Area: Figma and DESIGN.md. Status: resolved 2026-06-08.
25. The in-page save form and color is settled. The user chose to show the four-color picker in the in-page save form too, not only in the management panel (decision 2026-06-08). PRD.md FR-2, FR-3e, the Save form entry, and the Color picker entry now say both the in-page save form and the Management form panel show the color picker, with the first color selected by default for new entries. Area: request versus PRD.md. Status: resolved 2026-06-08.
26. The request and the mockup disagreed on which side the panel opens from. The written request said the panel opens from the left, but the Figma mockup (node 466-777) shows it on the right edge with a 16px margin. The user chose right, matching the mockup (decision 2026-06-08). PRD.md FR-17a and the Management form panel entry use the right side. Area: request versus Figma. Status: resolved 2026-06-08.
27. Immediate delete in the edit panel has no confirm and no undo. Claude raised the accidental-loss risk: the trash button sits beside Save, and one tap permanently removes the entry. The user chose to keep immediate delete with no confirm and no undo (decision 2026-06-08), recorded in PRD.md FR-18a. The UX risk stands but is accepted. Area: UX. Status: resolved 2026-06-08 (decision recorded).

20. The header did not match the Figma Page Nav mockup. The implementation showed "Glowsary" with a subtitle, left-aligned, while the Figma "Page Nav" frame (node 20:1601) showed a single centered title "Welcome to Glowsary!" with no subtitle above the tab nav. The user chose to match the Figma mockup (decision 2026-06-08). PRD.md FR-38, the Shared header entry, and the Management view entry were updated to the centered "Welcome to Glowsary!" title with no subtitle, and a build plan was issued to change the implementation. Area: Figma versus PRD.md and the implementation. Status: resolved 2026-06-08; implementation change handed to build mode.

16. The shared header subtitle reading slightly off on the Settings tab is now moot. The subtitle was removed entirely when the header changed to match the Figma mockup (see item 20), so it no longer shows on either tab. Area: UX and copy. Status: resolved 2026-06-08.

15. The Tab nav component was not specced. The redesign needs a Home/Settings tab nav, which was new: no DESIGN.md entry and not in the code. It is now specced in the DESIGN.md Components section as "Tab nav", built from the Figma tab nav frame, and the corner radius scale wording was reconciled from "the tab bar" to "the Tab nav". All colors map to existing tokens (`--bg-tertiary` track, `--bg-primary` active item, `--text-primary` label, `--fg-primary` icon) and both icons already ship in `extension/assets`. Open debt from this component is tracked separately in items 17 and 18. Area: DESIGN.md and code. Status: resolved 2026-06-08.

14. The management view two-tab split was not in PRD.md. The management view was defined as one page holding all pieces together. PRD.md now defines the two-tab layout: a shared header above a tab nav, Home as the default tab with the highlighting bar, Add new, and the saved words area, and Settings with the excluded sites manager (FR-38 to FR-41, and the updated component entries). Also renamed the "settings bar" to the "highlighting bar" to avoid confusion with the Settings tab (decision 2026-06-08). Area: request versus PRD.md. Status: resolved 2026-06-08.
